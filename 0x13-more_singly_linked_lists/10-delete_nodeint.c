/* Author:	Raymond Lukwago A.R */
#include "lists.h"

/**
* delete_nodeint_at_index -	Deletes node at specific position in linked lis
* @head:					Pointer to 1st node in linked list
* @index:					Index position to delete node
* Return:					Returns (1) if successful else (-1)
*/
int delete_nodeint_at_index(listint_t **head, unsigned int index);
{
	return (new_node);
}
