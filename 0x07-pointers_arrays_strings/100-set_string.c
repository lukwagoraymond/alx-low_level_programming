/* Author: Raymond Lukwago */

#include "main.h"
/**
* set_string - converts string of s
* @s: Pointer (s) pointing to address of to
* @to:	Pointer (to) pointing to address of
*		string value
*/
void set_string(char **s, char *to)
{
	*s = to;
}
